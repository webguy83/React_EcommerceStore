import React from 'react';

import { CheckoutContainer } from './Checkout.module.scss';

const Checkout = () => {
    return (
        <div className={CheckoutContainer}>
            <h1>Checkout</h1>
        </div>
    );
};

export default Checkout; 